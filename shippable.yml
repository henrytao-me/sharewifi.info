language: java

jdk:
  - oraclejdk8

# cache: true
# archive: true

install:
  - sudo apt-get update
  - sudo apt-get install -u lib32stdc++6 lib32z1
  - ./gradlew clean assemble

script:
  - ./gradlew test

notifications:
  email:
    recipients:
      - hungtaoquang@gmail.com
